# Portfolio views

`pinax board` renders the current repository's board from its log. `pinax
status` provides a concise current-state view. `pinax overview` combines
tracked repositories discovered under configured roots or listed in the
registry.

These commands calculate their output from current event logs. A Markdown
overview can be generated when a repository needs a committed, readable view.
